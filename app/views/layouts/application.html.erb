<!DOCTYPE html>
<html>
<head>
  <title>Canal de Denúncias</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "reports", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
  <header class="main-header">
    <div class="wrap header-flex">
      <div class="header-titles">
        <span class="app-logo">Canal de Denúncias</span>
        <h1><%= yield :header_title %></h1>
        <p><%= yield :header_desc %></p>
      </div>

      <nav class="header-nav" data-controller="dropdown">
        <button class="dropdown__btn" data-action="click->dropdown#toggle" type="button">
          <span>Menu</span>
          <span style="font-size: 0.7rem;">▼</span>
        </button>
              
 <div class="dropdown__menu" data-dropdown-target="menu" hidden>
  <div class="dropdown__header">Navegação</div>
  
  <%= link_to root_path, class: "dropdown__item" do %>
    <strong>Enviar Relato</strong>
    <small>Registrar nova ocorrência</small>
  <% end %>

          <%= link_to lookup_report_path, class: "dropdown__item" do %>
            <strong>Acompanhar Relato</strong>
            <small>Consultar status pelo protocolo</small>
          <% end %>

          <% if authenticated? %>
            <hr style="border: 0; border-top: 1px solid var(--border); margin: 0.5rem 0;">
            <%= link_to reports_path, class: "dropdown__item" do %>
              <strong>Administração</strong>
              <small>Gerenciar relatos recebidos</small>
            <% end %>
            
            <div class="dropdown__header">Sessão</div>
            
            <%= button_to session_path, 
                method: :delete, 
                class: "dropdown__item", 
                style: "width: 100%; text-align: left; background: none; border: none; cursor: pointer; display: block;" do %>
              <strong style="color: #dc2626;">Encerrar Sessão</strong>
              <small>Sair da conta de <%= Current.user.username %></small>
            <% end %>
          <% else %>
            <%= link_to new_session_path, class: "dropdown__item" do %>
              <strong>Login Administrativo</strong>
              <small>Acesso para membros da empresa</small>
            <% end %>
          <% end %>
        </div>
      </nav>
    </div>
  </header>

  <main class="wrap">
    
    <% if flash[:notice] %>
      <div class="badge badge--resolvido" style="width: 100%; margin-bottom: 1rem; padding: 1rem;">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div class="report-form__feedback" style="margin-bottom: 1rem;">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <%= yield %>
  </main>
</body>
</html>